# The GitHub Pages defaults already do the important part: each page's <title>
# comes from its own first heading, and relative .md links are rewritten to the
# built .html, so the same links work here and on github.com. Verified live on
# 2026-07-27 before this file existed. Do not add a theme here without checking
# the render again; the default one works.
#
# This file adds only what the defaults leave out.

title: invisible_playwright
description: >-
  Undetected Playwright automation on a stealth-patched Firefox. Notes on browser
  fingerprinting and bot detection, written from the detectors' own source and from
  our release gates.

url: https://feder-cr.github.io
baseurl: /invisible_playwright

plugins:
  - jekyll-sitemap
